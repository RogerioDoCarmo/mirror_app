# F-Droid Distribution Reference

How Miroji is packaged for [F-Droid](https://f-droid.org), why the setup looks
the way it does, and what happens on each release. F-Droid compiles every app
from source on its own build servers and signs the result with its own key —
which is fundamentally different from the Play Store / App Store flow, where we
upload a binary we built ourselves.

## The three repositories

| Repository                                                                   | Host   | Role                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [`mirror_app`](https://github.com/RogerioDoCarmo/mirror_app)                 | GitHub | The real project. TypeScript/React Native source, tests, CI, Expo config. The only F-Droid-specific piece here is the mirror workflow.                                                                                               |
| [`mirror_app-android`](https://github.com/RogerioDoCarmo/mirror_app-android) | GitHub | The **build mirror** F-Droid builds from: a plain-Gradle snapshot regenerated automatically on every release. Never edited by hand (except `README.md`, `.gitignore` and `fastlane/`, which the sync protects).                      |
| `miroji-fdroid-data`                                                         | GitLab | Fork of [`fdroid/fdroiddata`](https://gitlab.com/fdroid/fdroiddata). Carries one file — `metadata/com.rogeriodocarmo.miroji.yml`, the build recipe. The merge request from this fork into fdroiddata is the actual store submission. |

Submission links: [RFP issue #4133](https://gitlab.com/fdroid/rfp/-/work_items/4133)
· [Merge request !43165](https://gitlab.com/fdroid/fdroiddata/-/merge_requests/43165)

## Why a separate build mirror exists

Miroji is an Expo (managed workflow) app: the native `android/` project is not
committed here — it is generated by `expo prebuild`. F-Droid's build server
expects a repository it can build with standard tools. The mirror repo gives it
exactly that:

- the **generated `android/` Gradle project** (output of `expo prebuild`);
- the JS source, assets and config needed by Gradle's bundling step;
- `package.json` + `pnpm-lock.yaml` (the lockfile pins every dependency —
  `node_modules` itself is **not** committed; F-Droid installs it at build
  time with `pnpm install --frozen-lockfile` during the recipe's `init` step);
- an `.npmrc` with `node-linker=hoisted` (see below);
- Fastlane store metadata (`fastlane/metadata/android/<locale>/`), which
  F-Droid reads from the repo HEAD for the store listing title/descriptions.

### Why `node-linker=hoisted`

F-Droid's source scanner resolves **real paths**, but pnpm's default layout
symlinks every package through `node_modules/.pnpm/<name@version_peerhash>/`,
whose directory names change whenever the lockfile changes — impossible to
reference stably from the recipe's `scanignore`/`scandelete` lists (this
exactly failed in CI: every `scanignore` entry reported "Unused/Non-exist").
`node-linker=hoisted` makes pnpm produce a flat, symlink-free `node_modules`
(npm-style), so plain paths like
`node_modules/react-native/sdks/hermesc/linux64-bin` are real and match.

An earlier iteration vendored the full `node_modules` into the mirror
("vendoring" = committing third-party dependencies instead of downloading
them at build time) and was validated locally, but was dropped: it added 63k
committed files while the install-at-build-time flow is the standard shape
F-Droid reviewers see routinely.

## The prebuilt-binary problem (AARs) and its fix

An **AAR** (Android Archive) is the binary packaging format for an Android
library — compiled classes, resources, manifest fragment, sometimes native
`.so` libraries. Gradle either compiles a library module from source or
consumes a prebuilt AAR; Expo SDK ships each native module (expo-camera,
expo-splash-screen, …) with a `local-maven-repo/` folder containing the
module's **already-compiled AAR**, and consumes those by default to speed up
builds. That violates F-Droid's core policy: everything in the APK must be
compiled from source on their infrastructure.

The fix is Expo's official autolinking option, patched into the mirrored
`package.json` by the workflow:

```json
"expo": { "autolinking": { "android": { "buildFromSource": [".*"] } } }
```

Every entry is a regex matched against module names; `".*"` forces **all**
Expo/RN native modules to compile from source (Kotlin + C++/CMake), making the
prebuilt AARs dead weight. The recipe's `scandelete: node_modules` then lets
F-Droid's scanner delete them (and any other flagged blob) after the
build-time install, without breaking the build — nothing binary is committed
in the mirror itself.

Verified locally both ways: with the AARs physically deleted (build only
succeeds if source compilation genuinely works) and with them present but
ignored — both produce the same 1180-task build (~330 more than the
AAR-consuming build; the difference is the from-source compilation) and an APK
with the identical permission surface.

### hermesc — the one prebuilt that stays

**Hermes** is React Native's JavaScript engine. At build time the JS bundle is
ahead-of-time compiled to Hermes bytecode by a compiler binary called
`hermesc`, which react-native ships prebuilt for the three developer
platforms: `linux64-bin/`, `osx-bin/`, `win64-bin/`. Only the binary matching
the build machine's OS is ever executed. F-Droid builds on Linux: the scanner
deletes `win64-bin`/`osx-bin` via `scandelete`, while `linux64-bin` is
protected by `scanignore` because the build genuinely needs it. It is a
**build tool** — it never ships inside the APK — which is why it is
acceptable under `scanignore` while prebuilt app code (the AARs) is not.

## Release flow, end to end

1. A GitHub release `vX.Y.Z` is published on `mirror_app` (or the
   `android-mirror` workflow is dispatched manually with a `tag` input).
2. The workflow checks out the tag, runs `pnpm install` + `expo prebuild`,
   patches the tag-derived `versionCode` (`major*10000 + minor*100 + patch`,
   e.g. `v1.3.1` → `10301` — required because `expo prebuild` outside EAS
   always emits a placeholder `versionCode 1`), patches `buildFromSource`,
   writes the hoisted-layout `.npmrc`, and pushes the snapshot (without
   `node_modules`) to `mirror_app-android` with a matching tag.
3. F-Droid's `checkupdates` sees the new tag (`AutoUpdateMode: Version` /
   `UpdateCheckMode: Tags`), builds from the pinned commit per the recipe,
   signs with F-Droid's key, and publishes.
4. Store listing texts come from `fastlane/metadata/android/` at the mirror
   repo's HEAD — they can be updated any time without a release.

## Maintenance notes

- The recipe pins a **full commit hash** (maintainer requirement — never a tag
  or branch name). Re-mirroring a tag rewrites the mirror's history for that
  snapshot, so the hash in the recipe must be updated afterwards.
- Node comes from Debian (`apt-get install -y nodejs`) — the buildserver is
  Debian trixie, whose Node satisfies Expo's requirement (maintainer
  requirement — no downloaded tarballs).
- The F-Droid APK is a **different binary** from the Play Store one: F-Droid's
  signature, from-source native modules, tag-derived versionCode. Users cannot
  cross-update between the two installs.
