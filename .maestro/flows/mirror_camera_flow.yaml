appId: com.mirrorapp.app
---
# Pre-grant camera permission at the OS level so this flow is independent
# of mirror_permission_flow and always starts on the camera screen.
- launchApp:
    clearState: true
    permissions:
      camera: allow

- assertVisible:
    id: 'mirror-container'

- assertVisible:
    id: 'camera-view'
